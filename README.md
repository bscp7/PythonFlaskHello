# Python Flask Hello

Simple Python (Flask web framework) server that reads header named `message` and returns JSON response with `message` in it.

It add memory and cpu spikes that can help us see random usage metrics.
